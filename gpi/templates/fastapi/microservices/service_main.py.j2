"""Service {{ service_name }} — {{ project_name }}.

Généré par GPI v2
"""

from fastapi import FastAPI

app = FastAPI(title="{{ project_name }} - {{ service_name }}", version="1.0.0")


@app.get("/health")
def health():
    return {"status": "ok", "service": "{{ service_name }}"}
